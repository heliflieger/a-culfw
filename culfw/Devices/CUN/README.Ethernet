In order to make Ethernet work you need to grap svn sources from:

svn checkout http://avr-uip.googlecode.com/svn/trunk/ avr-uip-read-only

Than create a symbolic link within CUN directory to point to it:

ln -s /some/where/you/graped/avr-uip-read-only avr-uip



